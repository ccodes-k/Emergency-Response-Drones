using UnityEngine;

public class BaseStation : MonoBehaviour
{
    // Empty marker for now — SwarmManager queries stations for spawn points
    // Tag this object "Station". Place them around your map.
}
